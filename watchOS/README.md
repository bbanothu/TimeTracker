# Watch sources

The Watch companion lives next to the Expo iOS project (already wired in Xcode):

`app/ios/TimeTrackerWatch Watch App/`

This folder is intentionally empty so we don’t maintain two copies.
